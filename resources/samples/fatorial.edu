
int fatorial(int n) {
	int retorno;
	
	if (n > 1) {
		retorno = fatorial(n2);
	} else {
		retorno = 1;
	}
	
	return retorno;
}

main {

	int a, int b, int n, int resultado;

	a = 10;
	b = 5;
	
	n = a + b;
	
	if (n > 0) {
		resultado = fatorial(n);
		print("fatorial de " + n + " = " + resultado);
	}

}