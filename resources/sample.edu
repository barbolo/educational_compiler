functions

int fatorial(int n) {
	int retorno;
	begin
	
	if (n > 1) {
		retorno = fatorial(n);
	} else {
		retorno = 1;
	}
	
	return retorno;
}

main {

	int a, int b, int n, int resultado;
	begin
	a = 10;
	b = 5;
	
	n = a + b;
	
	if (resultado > 0) {
		resultado = fatorial(n);

	}

}